This folder is intended to store trained machine learning models generated by the electricity theft detection framework.

The proposed framework uses a hybrid Random Forest and Multi-Layer Perceptron (RF-MLP) ensemble model.

Future trained model files may include:

- Random Forest classifier
- Multi-Layer Perceptron classifier
- Hybrid model configuration and threshold parameters

Models can be generated by running the training pipeline available in the `src` directory.
